# Deterministic Hooks

- `destructive-command-guard` — destructive/irreversible command controls.
- `secret-guard` — unsafe secret handling controls.
- `verification-guard` — required verification controls.
- `git-guard` — history rewrites, force operations, shared-remote pushes, and branch-topology changes.

Hooks enforce rules; they do not redefine the constitution.
