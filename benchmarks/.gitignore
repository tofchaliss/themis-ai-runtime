# Build artifacts
bin/

# Machine-specific model registry (see models.example.json)
models.json

# Raw model outputs (regenerate with `themis-bench run`)
runs/

# Generated reports (regenerate with `themis-bench report`)
reports/

# Normalized responses and validation results (regenerate with
# `themis-bench evaluate` / `themis-bench validate`)
responses/
validation/
