Review the following Python code for security vulnerabilities.

```python
import os

username = input("Username: ")
password = input("Password: ")

os.system("echo Authenticating " + username)

query = "SELECT * FROM users WHERE username='" + username + "' AND password='" + password + "'"
print(query)
```

Include:

- Security vulnerabilities present
- Severity of each issue
- Why each issue is dangerous
- Recommended remediation
- Provide a secure implementation approach without rewriting the entire program
