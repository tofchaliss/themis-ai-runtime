# Layer 6 Traceability — grill invariants → tests

Tests in `src/harness/state`. Invariants from `design.md` §2 (D-L6-1..11), §3 (Q-L6-1..11 closures), and the implementation amendments 1–8.

| Invariant | Evidence |
| --- | --- |
| Two-sided eligibility: closed object/event vocabularies refuse the unknown; recovery/primitive-owned classes not caller-appendable; secrets flagged never edited/refused (D-L6-1) | `TestClosedVocabularies`, `TestContaminationFlagOnly` (flag AND verbatim), `TestBindArtifactDoor` (EvArtifact primitive-owned) |
| DurableCommit before ack; a durable record references only already-durable material — dangles die at the append door; orphans legal (D-L6-2) | `TestEventPlaneBehavior` (dangling ref refused typed; unknown ref class refused), `TestScanReachable` (orphan legal), fault-sweep universal 1 |
| Failed stream append terminal and sticky; frame cap refuses pre-write non-terminal (D-L6-2, amendment 5) | `TestFailedAppendTerminal`, `TestFrameCapRefusesPreWrite` |
| Object plane: idempotent dedup, verify-on-read, occupied-by-different-bytes, D-L6-11 publication — address only ABSENT or COMPLETE, retryable after every crash point | `TestObjectPlaneBehavior`, fault sweep retry assertion at all four object points |
| Identity: algorithm-prefixed objects (unknown prefix refuses), sink-assigned contiguous sequence, single-use task ids, path-shaped ids refused, **relocatable root**, constitution hash in every manifest (D-L6-3/5) | `TestIdentityStructure`, `TestConstitutionHash` |
| Three primitives only; exported API surface closed; no deletion path (os.Truncate pinned to recovery's frontier cut); no state verb in any registry (D-L6-4) | `TestExportedAPIClosure` (AST audit), `TestNoDeletionPathStructural`, `TestNoStateVerbInRegistries` (decoded scan) |
| Stream authoritative, manifest projection: event-before-projection; unexplainable manifest state = CORRUPT; lifecycle sequence must walk the machine; terminal binding (count both bounds + summary) branch-pinned; artifact list and attribution verifier-re-derived (D-L6-6, amendments 3/4) | `TestVerifierCatchesForgery`, `TestLifecycleSequenceLegality`, `TestTerminalBindingVerified` (summary and count in their own branches), `TestNegativeEventCountIsCorrupt` (verdict, never a panic), `TestProjectionReDerivation`, `TestJunkInsideFrameIsCorrupt`, `TestForgedFailedPartialBranch` (dedicated branch) |
| Runtime lifecycle enforcement through the API: illegal edges, terminal mutation, caller-requested FAILED_PARTIAL, bind-on-terminal all refuse typed (D-L6-6) | `TestIllegalTransitionsAPI`, `TestClosedVocabularies` (edge product + FP refusal) |
| Recovery: projection authority (reconciliation evidenced), structural fact (event-first FAILED_PARTIAL), zero origination, idempotent; refuses corruption, laundering (manifest ahead of stream), and live writers; torn tails preserved byte-exact then relocated; CreateTask crash window projects attribution from the CREATED event (D-L6-6, amendments 1/4; security HIGH-1) | `TestRecoveryAuthorities`, `TestRecoveryRefusesLaundering`, `TestRecoverRefusesLiveWriter`, `TestTornTail`, `TestCorruptEntry`, `TestCreateTaskCrashWindow`, `TestRecoveryCrashWindows` (recovery's own crash windows idempotent) |
| Reachability: derived from the record graph at scan time; shared objects reached; orphans left; torn/corrupt renders the scan INCOMPLETE and pins (D-L6-7) | `TestScanReachable`, `TestNegativeEventCountIsCorrupt` (corrupt → incomplete) |
| Root disjointness: absolute, existing, symlink-resolved, case-folded, pairwise both directions, ≥2 roots (D-L6-4 + hardening) | `TestCheckDisjointRoots` |
| StatusView structurally content-free; bytes only via event Resolve (D-L6-9) | `TestStatusViewContentFree`, `TestExportedAPIClosure` |
| Register C — fault injection: exhaustive 9-point sweep (universals: no dangling acknowledged record; recovery never originates), fault list synced to source literals by meta-test | `TestFaultPointSweepExhaustive`, `TestFaultPointsSyncWithSource` |
| Register C — real kill: poll-gated SIGKILL mid-task → typed FAILED_PARTIAL with recovery evidence, no dangles, idempotent second pass | `TestRealKillRecovery` |
| Register C — live proof: full L1→L5 task with L6 wired; record-before-effect on both legs (delivery record before the prompt reaches the model; audit before the result re-enters the loop); **byte-exact cold reconstruction** against events captured in-memory at emit; artifact + evidence round-trips; governed attribution survives cold | **`TestLiveTaskReconstruction` PASS vs qwen2.5:7b** (re-passed after every remediation; 11 events, VERIFIED) |
| L5 ArtifactStore inherited D-L6-11 publication (amendment 7) | `src/harness/execution` suite green post-change (store tests incl. immutability/tamper/idempotence) |

Reviews: combined security (2 HIGH + 3 MED + hardening → remediation commit 68ee4fc), test review (4 HIGH wrong-branch/dead-branch findings → branch-pinned regressions in review_test.go), architecture review (close-blocker 6a fixed; amendments 1–8 recorded in design.md). Coverage 83.9%. Residuals recorded: single-process writer exclusion, staging-temp accumulation, object-plane no-scan, read-auditing absent, L7-era wiring obligations (task assembly disjointness call site, production floor-event glue).
