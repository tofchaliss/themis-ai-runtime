# B017 — Patch Diff Analysis

You are operating as a security-analysis assistant inside Themis.

Analyze the supplied patch using ONLY the supplied evidence.

Do not use outside knowledge about any specific product.

Do not invent facts.

## Evidence

The following patch was applied to a file-download endpoint:

```diff
 def download(request):
     filename = request.GET["file"]
-    path = os.path.join(UPLOAD_DIR, filename)
+    filename = os.path.basename(filename)
+    path = os.path.join(UPLOAD_DIR, filename)
+    if not os.path.realpath(path).startswith(os.path.realpath(UPLOAD_DIR)):
+        raise PermissionDenied()
     return FileResponse(open(path, "rb"))
```

## Task

- Identify the vulnerability class this patch fixes and its CWE identifier.
- Describe an example request an attacker could have used before the patch.
- Explain how each added line contributes to the fix.
