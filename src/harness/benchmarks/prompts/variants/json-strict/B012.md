# B012 — CVE Fact Extraction (json-strict variant)

{{template "themis-preamble"}}

## Evidence

CVE: CVE-2024-99999

Description:
A vulnerability in Example Library allows a remote unauthenticated attacker
to cause memory corruption through a specially crafted network request.

CWE: CWE-119

Affected component:
example-library

Affected versions:
1.0.0 through 1.4.2

Fixed version:
1.4.3

CVSS:
Version: 3.1
Vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H
Score: 9.8
Severity: CRITICAL

Exploitability:
No public exploit information is provided in this evidence.

References:
- https://example.org/security/CVE-2024-99999
- https://example.org/advisory/99999

## Task

Extract the vulnerability facts into a single JSON object with exactly
these fields:

`cve`, `cwe`, `description`, `affected_component`, `affected_versions`
(array), `fixed_versions` (array), `cvss` (object with `version`,
`vector`, `score`, `severity`), `exploitability`, `references` (array),
`unknown_fields` (array).

Copy every value exactly as it appears in the evidence.

Output ONLY the JSON object. No prose. No code fences. No explanation.
